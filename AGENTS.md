# Repository instructions

- Never stage, commit, upload, or otherwise publish anything in `.local/`.
- `.local/` contains private test captures and may include personally identifiable information (PII).
- Keep `.local/` ignored by Git. Local analysis and validation may read its files without changing or publishing them.
